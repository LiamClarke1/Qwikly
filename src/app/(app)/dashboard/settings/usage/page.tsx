// Mounts the same UsagePage used at /dashboard/usage inside the settings
// shell so it inherits the settings sidebar nav. Owners can land here from
// either the top-level /dashboard/usage URL or from Settings → Usage &
// credits, both render identical content.
export { default } from "../../usage/page";

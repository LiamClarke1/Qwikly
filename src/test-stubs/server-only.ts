// Empty stub so vitest can resolve `import "server-only"` from server-side
// modules. In production builds this alias is unused — Next.js resolves
// `server-only` from node_modules and enforces the server/client boundary.
export {};
